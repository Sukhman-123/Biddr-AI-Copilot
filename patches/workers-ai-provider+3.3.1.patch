diff --git a/node_modules/workers-ai-provider/dist/index.mjs b/node_modules/workers-ai-provider/dist/index.mjs
index 6bd4682..ceb910f 100644
--- a/node_modules/workers-ai-provider/dist/index.mjs
+++ b/node_modules/workers-ai-provider/dist/index.mjs
@@ -688,7 +688,8 @@ function getMappedStream(response, salvageContext) {
 			if (choiceFinishReason != null) finishReason = mapWorkersAIFinishReason(choiceFinishReason);
 			else if (directFinishReason != null) finishReason = mapWorkersAIFinishReason(directFinishReason);
 			const nativeResponse = chunk.response;
-			if (nativeResponse != null && nativeResponse !== "") {
+			const hasNativeResponse = nativeResponse != null && nativeResponse !== "";
+			if (hasNativeResponse) {
 				const responseText = String(nativeResponse);
 				if (responseText.length > 0) if (bufferContentForSalvage) contentBuffer += responseText;
 				else {
@@ -740,7 +741,10 @@ function getMappedStream(response, salvageContext) {
 						delta: reasoningDelta
 					});
 				}
-				const textDelta = delta.content;
+				// Workers AI's Llama stream emits both native `response` and
+				// OpenAI-compatible `delta.content` with identical text. Prefer
+				// the native field whenever both are present.
+				const textDelta = hasNativeResponse ? void 0 : delta.content;
 				if (textDelta && textDelta.length > 0) if (bufferContentForSalvage) contentBuffer += textDelta;
 				else {
 					if (reasoningId) {
diff --git a/node_modules/workers-ai-provider/src/streaming.ts b/node_modules/workers-ai-provider/src/streaming.ts
index f1937cf..dd6b82a 100644
--- a/node_modules/workers-ai-provider/src/streaming.ts
+++ b/node_modules/workers-ai-provider/src/streaming.ts
@@ -165,7 +165,8 @@ export function getMappedStream(
 
 				// --- Native format: top-level `response` field ---
 				const nativeResponse = chunk.response;
-				if (nativeResponse != null && nativeResponse !== "") {
+				const hasNativeResponse = nativeResponse != null && nativeResponse !== "";
+				if (hasNativeResponse) {
 					const responseText = String(nativeResponse);
 					if (responseText.length > 0) {
 						if (bufferContentForSalvage) {
@@ -221,7 +222,12 @@ export function getMappedStream(
 						});
 					}
 
-					const textDelta = delta.content as string | undefined;
+					// Workers AI's Llama stream emits both native `response` and
+					// OpenAI-compatible `delta.content` with identical text. Prefer
+					// the native field whenever both are present.
+					const textDelta = hasNativeResponse
+						? undefined
+						: (delta.content as string | undefined);
 					if (textDelta && textDelta.length > 0) {
 						if (bufferContentForSalvage) {
 							contentBuffer += textDelta;
