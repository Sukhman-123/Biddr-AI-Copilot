# Biddr uses the Workers AI binding and does not require a third-party model key.
# Authenticate local remote bindings with `npx wrangler login`, or provide a
# scoped Cloudflare token in an untracked `.env`/`.dev.vars` file when required.
CLOUDFLARE_API_TOKEN=

